#!/bin/bash
# Mainnet deployment placeholder.
#
# Mainnet deployment is intentionally paused. This script is a non-executable stub
# so production and recovery flows cannot trigger deployment.

set -euo pipefail

echo "Mainnet deployment is paused."
echo "Path forward: complete the dress rehearsal, security audits, and explicit release approval before any future deployment window."
exit 1
