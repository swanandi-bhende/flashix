Place pre-trained model pickles here for local TEE testing.

Expected model filename: `arbitrage_scorer_v1.pkl`
Set the expected sha256 hash in `TEE_LOCAL_MODEL_HASH` environment variable or `.env.local` for local runs.
